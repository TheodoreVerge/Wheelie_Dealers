<h1>🚲 Wheelie Dealers 🚲</h1>
<ul><% @bikes.each do |bike| %>
<li><%= link_to bike.title, bike_path(bike) %>
<%= bike.title %>
<%= bike.price %>
<%= bike.brand %></li>
<% end %>
<br>
<br>
<p><%=link_to "Add a new bike", new_bike_path%> </p>
</ul>
